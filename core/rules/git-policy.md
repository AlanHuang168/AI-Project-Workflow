# Git Policy

- Do not run commit, push, merge, rebase, or force push without explicit user approval.
- Do not use Git operations to hide failures or incomplete work.
- Do not reset, overwrite, or delete user changes that are outside the approved task.
- If history rewriting or destructive Git operations are required, explain the risk and wait for user confirmation first.
